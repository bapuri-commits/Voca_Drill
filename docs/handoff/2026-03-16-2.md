# Voca_Drill Handoff — 2026-03-16 (2차)

## 현재 상태

Phase 1 스키마 갱신 + Phase 2 웹앱(FastAPI + React) + Phase 3 VPS 배포 완료. `https://voca.syworkspace.cloud`에서 282단어(Day 01-05)로 학습 가능. SyOps 서비스 탭 연동 + 헬스체크 활성화.

## 작업 내용

- Phase 1 스키마 갱신: tested/important synonyms 분리, example_en/ko, frequency, BookTest 테이블, CLI 버그 4개 수정
- 데이터 추출 스크립트: split_pdf.py (Day 30개+테스트 자동 분할), extract_words.py (Opus), extract_tests.py, verify_data.py
- PDF 분할 오프셋 수정 (+4), Day 01-05 Opus 추출, Review TEST 6개 추출
- Phase 2-1 FastAPI 백엔드: 14개 엔드포인트, DB 세션 dependency injection
- Phase 2-2 React 프론트엔드: 카드 플립 학습(3D 애니메이션), 대시보드, 단어장, 통계
- Phase 3 배포: Dockerfile(멀티스테이지), docker-compose, nginx, VPS 배포, 데이터 import
- SyOps 연동: services.ts live 전환, registry.py 헬스체크 활성화(포트 8500), 프론트 재빌드
- DNS: Cloudflare voca.syworkspace.cloud A 레코드 추가

## 핵심 결정 & 맥락

- 방안 B 채택: SyOps 연동 X, Voca_Drill 단일 레포에 backend + frontend 독립 웹앱
- 포트 8500: STRUCTURE_REDESIGN.md에서 8201 예정이었으나, BotTycoon이 8400을 쓰고 있어 8500으로
- DB 세션 관리: 일반 API는 Depends(get_db)로 요청마다 open/close, 세션 API는 active_session dict에 DB를 함께 보관
- Opus 모델: 데이터 추출에 Sonnet 대신 Opus 사용 (정확도 우선, 1회성 작업)

## 다음 작업

- [ ] Day 06-30 추출: `python scripts/extract_words.py --start 6 --end 30` (~2시간)
- [ ] Final TEST 추출: `python scripts/extract_tests.py --final-only`
- [ ] 추출 완료 후 VPS import
- [ ] UI 다듬기: 객관식/역방향/타이핑 퀴즈 UI, 세션 설정 화면, 교재 테스트 페이지, PWA
- [ ] STRUCTURE_REDESIGN.md 포트 8201 -> 8500 반영

## 세션 컨텍스트

- **대화 주제**: Phase 1 스키마 갱신 → 데이터 추출 스크립트 → PDF 분할 → API 추출(Day 01-05) → Phase 2 웹앱 → Phase 3 배포 → SyOps 연동
- **주요 논의 흐름**: 교재 분석 결과 반영 → CLI 임포트 테스트 → FastAPI 백엔드(DB 세션 누수 수정) → React 카드 플립 UI(stale closure 수정) → Docker 배포(python-multipart 누락) → SyOps 서비스 탭 연결
- **마지막 작업**: SyOps 헬스체크 확인 (status: up, 36ms)
- **참고**: UI는 MVP 수준. 카드 플립만 구현, 객관식/타이핑 등 추가 퀴즈 UI 미구현. Day 06-30 데이터 미추출. Voca_Drill 포트를 8500으로 변경했으므로 STRUCTURE_REDESIGN.md 업데이트 필요.
